# Development Database - Local SQLite
DATABASE_URI=sqlite:./data/dev-database.db

DATABASE_AUTH_TOKEN=""

# Blob storage (not needed for local development)
BLOB_READ_WRITE_TOKEN=""

# Payload Configuration
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
PAYLOAD_SECRET="your-super-secret-dev-key-change-this-to-something-random"

# Vercel OIDC (not needed for local development)
VERCEL_OIDC_TOKEN=""

# Development settings
NODE_ENV=development